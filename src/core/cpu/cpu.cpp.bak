#include "core/cpu/cpu.h"

#include <cstdint>
#include <iostream>

namespace gba {

static uint64_t g_instruction_count = 0;

Cpu::Cpu(Bus& bus) : bus_(bus) {
    Reset();
}

void Cpu::Reset() {
    r...[truncated]